Resume from current files and git history, not from scratch. The completed implementation commits are f5f9445 (assessments), 46349eb (tutorials), b8f3abf (slides), cfa491e (navigation/walkthroughs) and 2d00320 (planner/spec). Check current state in case further work has landed. At review, only research/export_sources.py was untracked; preserve it. src/data/sources.json has already been committed.

Finish the design-contract alignment that was interrupted. CLAUDE.md still states 8% quizzes. Align research/course-design.md and the project harness with the implemented contract: five 4% quizzes in weeks 3/5/8/10/12, ten 2% tutorial checkpoints in weeks 2–11, A1 Year-long Measurement Programme 20% in week 7, A2 Household Resilience Investment Proposal 40% at its existing exam-period date; twelve linked decks; three navigation groups; four walkthroughs; the shared deadline/effort planner. Remove the obsolete image-free comment in src/site-config.ts; conceptual imagery is the next phase, not a reason to remove technical diagrams. Preserve caveats and existing model constants.

Fix one observed content problem: tutorial 1 still says 'no preset is easier than another' and implies the model established that. Replace it with the qualified position used in the method: homes have different constraints; reference bands contextualise evidence, and sensitivity tests do not prove equal grading. Check for equivalent unsupported claims in week 1's notes/deck. Do not start new research.

Complete only outstanding validation from the previous session. Earlier logs report 89 tests, 12 compiled decks and browser work on both planner views/four tours; distinguish those recorded checks from what you personally repeat. Verify grouped navigation remains legible when desktop links wrap and on mobile. Group labels currently come from CSS pseudo-elements; check their accessibility and association with links, and use real labelled structure if needed rather than assuming CSS text creates semantic groups. Confirm all four tours' next/back/close/restart, keyboard focus and section links work, and planner totals/dates are correct. Inspect all twelve built decks for obvious clipping, and finish any missing mobile/desktop checks. Do not replace this with tests that only assert strings exist.

Run one tutorial-1 workbook example using the supplied CSVs and page instructions; resolve any mismatched row/column descriptions. Read week 3's overview/notes/slides/tutorial/quiz and the week 6–7 A1 handover together. Keep fixes small. No new dependencies, new scientific research, model changes or image generation.

Run mise exec -- pnpm check and mise exec -- pnpm check:evidence. PROCESS.md has a template comment and fake hashes a1b2c3d/e4f5a6b: report these student-authored blockers without editing or bypassing them. Commit the final contract alignment and relevant fixes with a descriptive message after the gate passes. Preserve prior commits.

Report exact checks and limitations, final hashes, any genuine omissions, preview instructions, and at most six illustration slots (page/purpose/aspect ratio/caption) for the next phase. Do not claim visual checks you did not perform. Stop after the completed local handoff; no publication.
